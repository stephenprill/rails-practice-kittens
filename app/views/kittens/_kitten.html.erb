<div class="kitten">
  <%= link_to image_tag(kitten.image), new_kitten_categorization_path(kitten), class: "kitten-link" %>
  <% kitten.categories.each do |category| %>
  <ul>
    <li><%= category.name %></li>
  </ul>
  <% end %>
</div>
